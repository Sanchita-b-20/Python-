class Solution:
    def climbStairs(self, n):

        # Base cases
        if n <= 2:
            return n

        first = 1
        second = 2

        # Calculate ways from step 3 to n
        for i in range(3, n + 1):
            current = first + second
            first = second
            second = current

        return second
